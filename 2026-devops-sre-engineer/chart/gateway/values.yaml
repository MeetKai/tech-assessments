replicaCount: 2

image:
  repository: registry.interno.meetkai.local/plataforma/gateway
  # Vazio usa o appVersion do chart.
  tag: ""
  pullPolicy: IfNotPresent

service:
  port: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 256Mi
